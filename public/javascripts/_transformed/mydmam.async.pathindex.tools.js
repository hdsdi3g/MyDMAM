/** This file is automatically generated! Do not edit. */ (function(pathindex) { /*
 * This file is part of MyDMAM.
 * 
 * This program is free software; you can redistribute it and/or modify
 * it under the terms of the GNU Lesser General Public License as published by
 * the Free Software Foundation; either version 3 of the License, or
 * any later version.
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 * GNU Lesser General Public License for more details.
 * 
 * Copyright (C) hdsdi3g for hd3g.tv 2015
 * 
*/

pathindex.resolveExternalPosition = function(externalpos_request_keys, data_callback) {
	if (!externalpos_request_keys) {
		return;
	}
	if (externalpos_request_keys.length == 0) {
		return;
	}
	$.ajax({
		url: mydmam.metadatas.url.resolvepositions,
		type: "POST",
		data: {
			"keys": externalpos_request_keys,
		},
		success: data_callback,
		error: function(jqXHR, textStatus, errorThrown) {
			console.error(jqXHR, textStatus, errorThrown);
		},
	});
};

pathindex.reactStoragePathLink = React.createClass({displayName: "reactStoragePathLink",
	render: function() {
		var url_navigate = mydmam.metadatas.url.navigate_react;

		var storagename = this.props.storagename;
		var path = this.props.path;
		var add_link = this.props.add_link;

		var storage_linked = storagename;
		if (add_link) {
			storage_linked = (React.createElement("a", {href: url_navigate + "#" + storagename + ":/"}, storagename));
		}		

		var path_linked = path;
		if (add_link) {
			path_linked = [];
			var sub_paths = path.split("/");
			var sub_path;
			var currentpath = "";
			for (var i = 1; i < sub_paths.length; i++) {
				sub_path = sub_paths[i];
				path_linked.push(
					React.createElement("span", {key: i}, "/", 
						React.createElement("a", {href: url_navigate + "#" + storagename + ':' + currentpath + "/" + sub_path}, 
							sub_path
						)
					)
				);
				currentpath = currentpath + "/" + sub_path;
			};
		}

		return (
			React.createElement("span", null, 
				React.createElement("strong", {className: "storagename"}, 
					storage_linked
				), 
				" :: ", 
				React.createElement("span", {className: "path"}, 
					path_linked
				)
			)
		);
	}
});

pathindex.reactFileSize = React.createClass({displayName: "reactFileSize",
	render: function() {
		if (!this.props.size) {
			return null;
		}
		var style = {marginLeft: 5};
		if (this.props.style) {
			style = this.props.style;
		}
		return (React.createElement("span", {className: "label label-important", style: style}, React.createElement("i", {className: "icon-briefcase icon-white"}), " ", mydmam.format.number(this.props.size)));
	},
});

pathindex.reactDate = React.createClass({displayName: "reactDate",
	render: function() {
		if (!this.props.date) {
			return null;
		}
		if (this.props.date == 0) {
			return null;
		}
		var label = null;
		if (this.props.i18nlabel != null) {
			label = i18n(this.props.i18nlabel) + " ";
		}
		var style = {marginLeft: 5};
		if (this.props.style != null) {
			style = this.props.style;
		}
		return (React.createElement("span", {className: "label", style: style}, label, mydmam.format.fulldate(this.props.date)));
	},
});

pathindex.reactSinceDate = React.createClass({displayName: "reactSinceDate",
	render: function() {
		if (!this.props.date) {
			return null;
		}
		if (this.props.date == 0) {
			return null;
		}
		var label = null;
		if (this.props.i18nlabel) {
			label = i18n(this.props.i18nlabel) + " ";
		}
		var style = {marginLeft: 5};
		if (this.props.style) {
			style = this.props.style;
		}

		var since = mydmam.format.secondsToYWDHMS((new Date().getTime() - this.props.date) / 1000);
		if (since < 0) {
			return React.createElement(pathindex.reactDate, {date: this.props.date, i18nlabel: this.props.i18nlabel, style: this.props.style});
		}

		return (React.createElement("span", {className: "label", style: style}, 
			React.createElement("i", {className: "icon-time icon-white", style: {marginTop: 0}}), " ", label, since
		));
	},
});

/**
 * Transform "application/x-dummy" to "application-dummy", and translate it.
 */
pathindex.mtdTypeofElement = function(mtd_element) {
	if (!mtd_element) {
		return "";
	}
	if (!mtd_element.mimetype) {
		return "";
	}
	var element = mtd_element.mimetype;
	var element_type = element.substr(0, element.indexOf('/'));
	var element_subtype = element.substr(element.indexOf('/') + 1);

	if (element_subtype.startsWith("x-")) {
		element_subtype = element_subtype.substr(2);
	}
	element = element_type + "-" + element_subtype;

	var translated_element = i18n("mime." + element);
	if (translated_element.startsWith("mime.")) {
		translated_element = translated_element.substr(5);
	}
	return translated_element;
};

pathindex.reactBasketButton = React.createClass({displayName: "reactBasketButton",
	getInitialState: function() {
		return {present_in_basket: mydmam.basket.isInBasket(this.props.pathindexkey)};
	},
	handleBasketSwitch: function(event) {
		if (this.state.present_in_basket) {
			mydmam.basket.content.remove(this.props.pathindexkey);
		} else {
			mydmam.basket.content.add(this.props.pathindexkey);
		}
		this.setState({present_in_basket: !this.state.present_in_basket});
	},
	render: function() {
		if (this.props.pathindexkey === md5('')) {
			return null;
		}
		var btn_basket_classes = classNames({
		    'btn': true, 'btn-mini': true, 'basket': true,
		    'active': this.state.present_in_basket,
		});

		var icon = (React.createElement("i", {className: "icon-star-empty"}));
		if (this.state.present_in_basket) {
			icon = (React.createElement("i", {className: "icon-star"}));
		}

		return (
			React.createElement("button", {className: btn_basket_classes, type: "button", onClick: this.handleBasketSwitch}, 
				icon
			)
		);
	},
});
})(window.mydmam.async.pathindex);
// Generated by hd3gtv.mydmam.web.JSProcessor the Sat Dec 26 03:36:04 CET 2015 with MyDMAM git version: dvl 6a57be20
