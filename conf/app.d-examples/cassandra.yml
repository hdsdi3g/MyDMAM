cassandra:
    clustername : mycluster
    rcp_cluster : 127.0.0.1
    keyspace : ks

# rcp_cluster can to be like :
# host
# host:specialport
# host1,host2
# host1:specialport,host2
# host1:host2:host3
# ...
